for from in nomtls mtls; do for to in nomtls mtls; do kubectl exec $(kubectl get pod -l app=curl -n ${from} -o jsonpath={.items..metadata.name}) -c curl -n ${from} -- curl http://hostname.${to}/healthz/ -s -o /dev/null -w "curl.${from} to hostname.${to}: %{http_code}\n"; done; done
